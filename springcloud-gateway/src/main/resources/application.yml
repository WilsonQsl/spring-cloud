server:
  port: 80
spring:
  application:
    name: gate-service-api
  cloud:
    gateway:
      routes:
        - id: gateway-service
          uri: http://listinfotech.com
          predicates:
            - Path=/category/javaee/nginx/
    nacos:
      config:
        server-addr: 127.0.0.1:8848
      discovery:
        server-addr: 127.0.0.1:8848